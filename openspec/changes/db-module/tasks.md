# Tasks — db-module

Порядок = граф зависимостей: 1.1 → 1.2 → 1.3 → 1.4 → 1.5 → **1.9** → 1.10 → 1.11 → 1.12 → 1.13 → 1.14 → 1.15. Задача 1.9 (пул соединений, решение человека 2026-08-20) реализуется ДО DAO-задач — свежие агенты пишут против финального API. **Решение человека 2026-08-20:** агент не выполняет задачу целиком → каждая DAO-задача разделена на пару «реализация» (модуль + lib.rs + smoke-тест) и «тесты» (полный набор по критериям приёмки). Каждая задача выполняется СВЕЖИМ агентом без памяти предыдущих — тело самодостаточно. Формат: чекбокс + блок деталей (цель / scope / зависимости / критерии приёмки / референс / история ревизий).

**Решения change (2026-08-19, одобрено человеком):** D1 пул соединений r2d2_sqlite (пересмотрено 2026-08-20: read-heavy нагрузка; max_size=4; PRAGMA на каждом соединении; `Db::lock()`/`conn()` удалены, вместо них `with_conn`); D2 транзакции — нативный rusqlite (`Connection::transaction()`/commit/rollback, авто-rollback через Drop) + closure-паттерн `exec_tx` + абстракция `DbExecutor`/`ConnectionOrTx`; D3 миграции — rusqlite_migration 2.6 (from-directory) + include_dir 0.7, squash `migrations/1-init/up.sql` из `.archive/spikes/migrations/`, user_version — единственный источник истины, `_schema_migrations` НЕ создаётся; D4 декомпозиция 10 Go-DAO → 8 Rust-модулей; D5 атомарные GetOrCreate/CreateOrIgnore через ON CONFLICT (исправление TOCTOU-гонки Go); D6 utils — локальный модуль db::utils (Normalize/EscapeLike); D7 vec0 полностью исключён; D8 PRAGMA-parity (WAL, synchronous=NORMAL, cache_size=-64000, mmap_size=268435456, foreign_keys=ON, busy_timeout=5000); D9 параметр-лимит SQLite 32766 — батчи ≤ 500 строк; D10 вложенный exec_tx → DbError::NestedTransaction (thread-local IN_TX); D11 with_conn вместо lock(); D12 тест-инфраструктура под пул (shared-cache :memory:).

**Принцип миграции (решение человека 2026-08-19, binding):** НЕ транскрибировать Go 1:1 — цель функциональная копия, не кодовая; архитектурно правильно и оптимально для Rust (DRY, KISS, SOLID, YAGNI); совместимость на внутреннем уровне не требуется; баги Go исправлять или фиксировать осознанное отклонение в отчёте.

## 1. Крейт db

- [x] 1.1 Инфраструктура: соединение, миграции, транзакции, ошибки, utils, тест-утилиты
  - **Цель:** фундамент крейта `db`: открытие соединения с PRAGMA-parity (D8), применение squash-миграции (D3), транзакционный слой (D2), типы ошибок, утилиты (D6), тест-утилиты. **НЕ транскрибировать Go 1:1** (принцип миграции): цель — функциональная копия, не кодовая; каждое правило оракула проанализировать — баги Go исправить или зафиксировать осознанное отклонение в отчёте.
  - **Scope файлов:** `crates/db/Cargo.toml` (deps: `rusqlite` workspace (bundled+fts5), `rusqlite_migration` 2.6 с feature `from-directory`, `include_dir` 0.7, `thiserror` workspace — версии/MSRV/CVE проверить онлайн реализатором, пин с комментарием), корневой `Cargo.toml` (добавить rusqlite_migration + include_dir в палитру с комментарием о проверке), `migrations/1-init/up.sql` (КОПИЯ из `.archive/spikes/migrations/1-init/up.sql` verbatim — squash v5 DDL, уже проверен в спайке S1), `crates/db/src/lib.rs` (crate docs + модули + re-exports), `crates/db/src/connection.rs` (`Db` struct: `open(path) -> Result<Db, DbError>` — открытие + PRAGMA-применение + миграция; `exec_tx<T, E>(f: impl FnOnce(&mut Transaction) -> Result<T, E>) -> Result<T, E>` — closure-транзакция на нативном API rusqlite, авто-rollback через Drop; `conn()`/`lock()` для DAO), `crates/db/src/executor.rs` (`DbExecutor` sealed trait: execute/query/query_row + `ConnectionOrTx<'a>` enum, реализующий trait для Connection и Transaction), `crates/db/src/error.rs` (`DbError` через thiserror: Io{path,source}, Sqlite{source}, Migration{source}, Poisoned — варианты для будущих задач), `crates/db/src/utils.rs` (`normalize(s) -> String` — trim+lowercase+collapse whitespace; `escape_like(s) -> String` — escape `\`, `%`, `_`), `crates/db/src/test_util.rs` (`in_memory_db() -> Db` — :memory: база с миграциями и PRAGMA; `fixture_db() -> Db` — открытие `fixtures/knowledge.db` в mode=ro/immutable для parity-тестов; `insert_test_document/chunk/entity` хелперы по мере необходимости).
  - **Зависимости:** config (пути/PRAGMA-настройки — только как референс, db не зависит от config на уровне кода в этой задаче). Новые deps: rusqlite_migration, include_dir (палитра).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) `open` на несуществующий файл создаёт базу с полной v5-схемой, `PRAGMA user_version` = 1, `_schema_migrations` отсутствует (проверка sqlite_master); (б) PRAGMA-parity: journal_mode=wal, synchronous=NORMAL, foreign_keys=ON, busy_timeout=5000, cache_size=-64000, mmap_size=268435456; (в) повторное открытие той же базы не перезапускает миграции (user_version=1, данные сохраняются); (г) `exec_tx`: успех → COMMIT (данные видны после), ошибка → ROLLBACK (данных нет), паника → ROLLBACK (Drop-семантика); (д) `normalize`/`escape_like` unit-тесты (краевые случаи: пустая строка, множественные пробелы, спецсимволы LIKE); (е) `in_memory_db` создаёт рабочую базу с миграциями. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/database.go` (Open, PRAGMA-настройки, Close), `../synopsis/internal/database/schema.go` (схема), `../synopsis/internal/database/dao/tx.go` + `dbtx.go` (TxManager/DBTX — семантика транзакций), `.archive/spikes/migrations/1-init/up.sql` (squash DDL), `.archive/spikes/` (S1-спайк: rusqlite_migration/include_dir API-паттерн).
  - **История ревизий:**
    - Ревизия 1 (2026-08-19): первая версия.

- [x] 1.2 AppKV DAO
  - **Цель:** простейший CRUD над таблицей `app_kv` (ключ-значение: last_linking_run и пр.). **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/app_kv.rs` (`AppKv` struct + `get(&self, key) -> Result<Option<String>, DbError>` + `set(&self, key, value) -> Result<(), DbError>`), `crates/db/src/lib.rs` (модуль + re-export), тесты в `crates/db/src/app_kv.rs` (`#[cfg(test)]`).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, test_util).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) set+get round-trip; (б) get отсутствующего ключа → None; (в) set перезаписывает значение; (г) работа через `exec_tx` (set внутри транзакции, откат при ошибке). fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/app_kv.go` (+ app_kv_test.go) — семантика Get/Set.
  - **История ревизий:**
    - Ревизия 1 (2026-08-19): первая версия.

- [x] 1.3 Document DAO
  - **Цель:** CRUD + пагинация + доменные операции над таблицей `documents`. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/document.rs` (`Document` struct (id, path, source_type, metadata_json, content_hash, created_at, updated_at — домен НЕ колонка в v5-схеме, живёт в metadata_json как $.domain; колонок title/indexed_at в v5 нет), `DocumentDao` struct + методы: `create`, `get_by_id`, `get_by_path`, `get_by_ids` (IN-список с плейсхолдерами, батчи ≤ 500), `update_hash`, `list` (пагинация с фильтрами domain через json_each, source_type, name через LIKE с escape_like), `delete`, `count`, `list_paginated`, `documents_by_type`, `unique_domains`), `crates/db/src/lib.rs` (модуль + re-export), тесты в `crates/db/src/document.rs` (`#[cfg(test)]`).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, utils::escape_like, test_util).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) create+get_by_id round-trip; (б) get_by_path; (в) get_by_ids с несколькими ID; (г) update_hash меняет content_hash; (д) delete удаляет и повторный get → None; (е) list_paginated с фильтрами domain/source_type/name (LIKE с escape_like — спецсимволы в имени не ломают запрос); (ж) unique_domains возвращает уникальные домены из metadata_json (json_each); (з) count корректно учитывает фильтры. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/document_dao.go` (+ document_dao_test.go) — семантика операций, SQL-паттерны (json_each для domain, LIKE-фильтры).
  - **История ревизий:**
    - Ревизия 1 (2026-08-19): первая версия.

- [x] 1.4 Chunk DAO + FTS5-поиск
  - **Цель:** CRUD над таблицей `chunks` + FTS5-поиск с bm25-ранжированием и domain-фильтром. **vec0-операции полностью исключены (D7)** — SearchVector, UpsertVector, FormatVector, DeleteVectorsByChunkIDs, DeleteOrphanedVectors НЕ переносятся (переезжают в change `vectors`). **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/chunk.rs` (`Chunk` struct (id, doc_id, chunk_text, sequence_num, start_offset, end_offset, created_at — поля по v5-схеме; token_count/metadata_json/updated_at/chunk_index в v5 НЕТ), `ChunkDao` struct + методы: `create`, `get_by_id`, `list_by_doc_id`, `update`, `delete`, `count_by_doc_id`, `count`, `search_fts` (FTS5 MATCH + bm25() ранжирование + опциональный domain-фильтр через json_each на documents.metadata_json), `delete_by_ids` (IN-список, батчи ≤ 500), `list_all`), `crates/db/src/lib.rs` (модуль + re-export), тесты в `crates/db/src/chunk.rs` (`#[cfg(test)]`) + интеграционный parity-тест в `crates/db/tests/fts5_parity.rs` (фикстура `fixtures/knowledge.db`).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, test_util::fixture_db), 1.3 (Document — для domain-фильтра через documents.metadata_json).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) CRUD round-trip; (б) list_by_doc_id; (в) delete_by_ids; (г) **FTS5-parity на фикстуре**: запрос 'knowledge' → 17 хитов, top-3 chunk_id совпадают с оракулом (значения из спайка S1: 247, 30, 106 — проверить реализатором на фикстуре и зафиксировать в тесте); (д) search_fts с domain-фильтром возвращает только чанки документов домена; (е) синхронизация индекса: create/update/delete чанка отражается в поиске (триггеры ai/ad/au); (ж) в коде крейта db нет ссылок на vec0 (grep-проверка). fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/chunk_dao.go` (+ chunk_dao_test.go) — семантика CRUD и SearchFTS (bm25 + json_each domain-фильтр), `../synopsis/internal/database/e2e_vec_test.go` (НЕ переносить vec0-части), `.archive/spikes/` (S1: FTS5-паттерн, bm25-запрос, top-3 chunk_ids).
  - **История ревизий:**
    - Ревизия 1 (2026-08-19): первая версия.

- [x] 1.5 Entity DAO
  - **Цель:** CRUD + атомарный GetOrCreate + batch + orphan-cleanup + пагинация над таблицей `entities`. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/entity.rs` (`Entity` struct (id, entity_type, name, domain, description, metadata_json, created_at, updated_at), `EntityDao` struct + методы: `create`, `get_by_id`, `get_by_name` (type+name+domain), `get_by_name_fold` (normalize), `list_by_name_fold`, `list`, `list_by_type`, `update`, `update_name`, `delete`, `count`, `list_paginated` (фильтры type/domain/name), `get_or_create` (АТОМАРНЫЙ: `INSERT ... ON CONFLICT(type,name,domain) DO NOTHING` + возврат ID через RETURNING или повторный SELECT — D5, исправление TOCTOU-гонки Go), `delete_orphaned_entity_ids` (не удаляет EntityType и сущности, на которые ссылаются факты/связи), `delete_orphaned_by_ids`, `get_by_ids` (IN-список, батчи ≤ 500), `types_by_count`, `domains_by_count`, `unique_types`, `list_created_since`), `crates/db/src/lib.rs` (модуль + re-export), тесты в `crates/db/src/entity.rs` (`#[cfg(test)]`).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, utils::normalize, test_util).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) CRUD round-trip; (б) get_by_name/get_by_name_fold (normalize: регистр/пробелы); (в) **get_or_create атомарность**: два вызова с одинаковыми ключами → одна запись, оба возвращают один ID (тест с конкурентными вызовами или повторным вызовом); (г) list_paginated с фильтрами; (д) delete_orphaned_entity_ids не удаляет EntityType и референсируемые сущности; (е) get_by_ids с несколькими ID; (ж) types_by_count/domains_by_count/unique_types корректны; (з) list_created_since. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/entity_dao.go` (+ entity_dao_test.go, entity_dao_batch_test.go) — семантика операций, SQL-паттерны, orphan-cleanup логика.
  - **История ревизий:**
    - Ревизия 1 (2026-08-19): первая версия.
    - Ревизия 2 (2026-08-19): фикс по замечанию ревьюера (minor, API_consistency) — унифицировать порядок параметров `get_or_create` на `(entity_type, name, domain)` как в `create` (ревьюер: lines 154-161 vs 332-339).

- [x] 1.9 Пул соединений: r2d2_sqlite, with_conn, защита от вложенных транзакций
  - **Цель:** заменить `Arc<Mutex<Connection>>` на пул соединений `r2d2_sqlite` (решение человека 2026-08-20: приложение read-heavy — много чтений, мало записей; WAL + несколько соединений → конкурентные чтения, write-транзакции не блокируют чтения). Устранить деадлок-футган по построению: удалить `Db::lock()`/`Db::conn()` из публичного API, добавить `Db::with_conn(f)`; вложенный `exec_tx` → `Err(DbError::NestedTransaction)` через thread-local флаг (D10, D11, D12). DAO-структуры и `ConnectionOrTx` НЕ меняются. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** корневой `Cargo.toml` (добавить `r2d2` + `r2d2_sqlite` в палитру с комментарием о проверке версий/MSRV/CVE онлайн реализатором), `crates/db/Cargo.toml` (deps), `crates/db/src/connection.rs` (переписать: `Db { pool: r2d2::Pool<SqliteConnectionManager> }`; `open` — миграция один раз на первом соединении + PRAGMA-применение на КАЖДОМ соединении через init-колбэк менеджера (r2d2_sqlite `with_init` или кастомный ConnectionManager — проверить API); `with_conn<T>(&self, f: impl FnOnce(&Connection) -> T) -> Result<T, DbError>`; `exec_tx` — checkout из пула + транзакция + возврат соединения, thread-local `IN_TX` → `Err(DbError::NestedTransaction)`; `Db::lock()`/`conn()` УДАЛИТЬ; `Db::new` для fixture — пул max_size=1), `crates/db/src/error.rs` (+`NestedTransaction`, +`PoolTimeout`), `crates/db/src/test_util.rs` (`in_memory_db` — shared-cache URI `file:memdb1?mode=memory&cache=shared` + SQLITE_OPEN_URI, все соединения пула разделяют одну БД; `fixture_db` — пул max_size=1 над ro/immutable URI), `crates/db/src/lib.rs` (docs), ВСЕ тестовые модули DAO (app_kv/document/chunk/entity) + `crates/db/tests/fts5_parity.rs` (механическая миграция `db.lock().unwrap()` → `db.with_conn(...).unwrap()`).
  - **Зависимости:** 1.1–1.5 (весь текущий код db). Новые deps: r2d2, r2d2_sqlite (палитра).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) PRAGMA-parity на КАЖДОМ соединении: два checkout'а из пула оба читают WAL/synchronous/foreign_keys/busy_timeout/cache_size/mmap_size; (б) параллельные чтения: 8 потоков одновременно `with_conn` с запросами — все завершаются корректно без деадлока; (в) чтение во время write-транзакции: поток с `exec_tx` (вставка) + поток с чтением — чтение не блокируется (WAL); (г) exec_tx-семантика сохранена: commit/rollback/panic (существующие тесты проходят без изменений); (д) вложенный `exec_tx` → `Err(DbError::NestedTransaction)`, без деадлока (новый тест); (е) `Db::lock()`/`conn()` отсутствуют в публичном API (grep-проверка; старые вызывающие мигрированы); (ж) `with_conn` работает для чтений и одиночных записей; (з) `in_memory_db`: запись через один checkout видна через другой (одна общая БД); (и) `fixture_db` + parity-тесты проходят; (к) полный workspace: fmt/clippy/test чисто. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/database.go` (пул database/sql — семантика), design.md D1 (пересмотрено)/D10/D11/D12, текущий `crates/db/src/connection.rs` (что переписывается).
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): первая версия (решение человека 2026-08-20: Option B — пул, до задач 1.6–1.8).

- [x] 1.10 Fact DAO — реализация
  - **Цель:** реализовать модуль `fact.rs` (Fact struct + FactDao) и зарегистрировать в lib.rs. Полный тестовый набор — задача 1.11; здесь только smoke-тест (round-trip), доказывающий работоспособность. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/fact.rs` (`Fact` struct (id, subject_entity_id, object_entity_id, predicate, confidence, metadata_json, created_at, updated_at), `FactDao` struct + методы: `create`, `create_or_ignore` (АТОМАРНЫЙ: `INSERT ... ON CONFLICT(subject_entity_id, object_entity_id, predicate) DO NOTHING` + возврат ID через RETURNING — D5), `recompute_weights`, `get_by_id`, `list_by_entity_id`, `list_by_entity_ids` (два IN-списка subject+object, батчи ≤ 500), `list_all`, `find_orphaned_fact_ids`, `delete_orphaned_facts`, `validate_fact_domain` (normalize-сравнение доменов субъекта и объекта), `get_by_ids` (IN-список, батчи ≤ 500), `count`, `search_paginated` (JOIN с entities + фильтры), `delete`), `crates/db/src/lib.rs` (модуль + re-export), минимальный smoke-тест в `crates/db/src/fact.rs` (`#[cfg(test)]`: create + get_by_id round-trip).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, utils::normalize, test_util), 1.5 (Entity — для validate_fact_domain), 1.9 (пул API: with_conn/exec_tx).
  - **Критерии приёмки:** `cargo build -p db` и `cargo test -p db` зелёные (существующие тесты + smoke); fmt/clippy чисто; публичные API документированы (missing_docs=deny); grep-проверка: в новом коде нет ссылок на vec0.
  - **Референс:** `../synopsis/internal/database/dao/fact_dao.go` — семантика операций, SQL-паттерны, orphan-cleanup логика; design.md D5 (атомарный create_or_ignore); текущий `crates/db/src/entity.rs` — паттерн DAO (структура, ConnectionOrTx, стиль).
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.6 (решение человека 2026-08-20: агент не тянет задачу целиком — разделение реализация/тесты).

- [x] 1.11 Fact DAO — тесты
  - **Цель:** полный тестовый набор для `fact.rs` по критериям приёмки исходной задачи 1.6. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** только тесты в `crates/db/src/fact.rs` (`#[cfg(test)]`).
  - **Зависимости:** 1.10 (реализация fact.rs), 1.1 (test_util), 1.5 (Entity — для validate_fact_domain), 1.9 (пул API).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) CRUD round-trip; (б) **create_or_ignore атомарность**: повторный вызов с теми же ключами → одна запись, тот же ID; (в) list_by_entity_ids (subject+object); (г) validate_fact_domain: совпадающие домены → Ok(true), разные → Ok(false) (normalize; отсутствующий endpoint → Ok(false)); (д) find_orphaned_fact_ids/delete_orphaned_facts (не удаляет факты с живыми сущностями); (е) search_paginated с фильтрами (страница и total согласованы при совпадении имён обоих endpoint'ов); (ж) count; (з) recompute_weights (батчинг IN-списков). **API-факты из 1.10 (binding):** `Fact` struct по v5-схеме: (id, subject_entity_id, object_entity_id, predicate, domain, status, valid_from, valid_to, weight, metadata, created_at, updated_at) — колонки confidence в v5 НЕТ; `validate_fact_domain` → `Result<bool, DbError>`; `delete(id) -> bool`; create/create_or_ignore хранят status='approved'. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/fact_dao_test.go`, `fact_dao_batch_test.go` — семантика; design.md D5; модульная документация `crates/db/src/fact.rs` (зафиксированные исправления багов Go).
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.6.
    - Ревизия 2 (2026-08-20): API-факты из 1.10 (Fact по v5-схеме, validate_fact_domain → bool).

- [ ] 1.12 Relation DAOs — реализация
  - **Цель:** реализовать три модуля связей (chunk_entity, entity_link, entity_source) + регистрация в lib.rs. Полный тестовый набор — задача 1.13; здесь только smoke-тесты (базовый round-trip каждого DAO). **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/chunk_entity.rs` (`ChunkEntityDao`: `link` (INSERT OR IGNORE), `unlink`, `get_entities_by_chunk`, `get_chunks_by_entity`, `is_linked`, `unlink_chunk`, `unlink_entity`, `get_chunk_texts_by_entity`, `get_entities_by_chunks`, `get_entity_ids_by_doc_id`), `crates/db/src/entity_link.rs` (`EntityLinkDao`: `create` (отклоняет self-link), `list_by_entity`, `list_by_method`, `list_all`, `delete`, `count`, `graph_node_count`, `delete_by_entity_ids`), `crates/db/src/entity_source.rs` (`EntitySourceDao`: `create`, `link_batch` (multi-row INSERT OR IGNORE, батчи по 500 строк), `delete_by_document_id` (возвращает затронутые ID до удаления), `get_documents_by_entity_id`, `find_orphaned_entity_ids` (не удаляет EntityType)), `crates/db/src/lib.rs` (модули + re-exports), smoke-тесты в каждом модуле (`#[cfg(test)]`).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, test_util), 1.3 (Document), 1.4 (Chunk), 1.5 (Entity), 1.9 (пул API).
  - **Критерии приёмки:** `cargo build -p db` и `cargo test -p db` зелёные (существующие тесты + smoke); fmt/clippy чисто; публичные API документированы (missing_docs=deny); grep-проверка: нет ссылок на vec0.
  - **Референс:** `../synopsis/internal/database/dao/chunk_entity_dao.go`, `entity_link_dao.go`, `entity_source_dao.go` — семантика операций, SQL-паттерны, batch-логика; текущий `crates/db/src/entity.rs` — паттерн DAO.
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.7.

- [ ] 1.13 Relation DAOs — тесты
  - **Цель:** полный тестовый набор для трёх модулей связей по критериям приёмки исходной задачи 1.7. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** только тесты в `crates/db/src/chunk_entity.rs`, `crates/db/src/entity_link.rs`, `crates/db/src/entity_source.rs` (`#[cfg(test)]`).
  - **Зависимости:** 1.12 (реализация трёх модулей), 1.1 (test_util), 1.3–1.5 (Document/Chunk/Entity), 1.9 (пул API).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) link/unlink/is_linked round-trip; (б) get_entities_by_chunk/get_chunks_by_entity; (в) unlink_chunk/unlink_entity удаляют все связи; (г) get_chunk_texts_by_entity возвращает тексты чанков; (д) entity_link: create + self-link отклоняется; (е) list_by_entity/list_by_method/list_all; (ж) delete/delete_by_entity_ids (двунаправленное удаление); (з) graph_node_count; (и) entity_source: link_batch с 500+ строками (граница батча, параметр-лимит 32766 не нарушен); (к) delete_by_document_id возвращает ID до удаления; (л) find_orphaned_entity_ids не включает EntityType. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/chunk_entity_dao_test.go`, `entity_link_dao_test.go`, `entity_source_dao_test.go` — семантика.
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.7.

- [ ] 1.14 FactSource DAO — реализация
  - **Цель:** реализовать модуль `fact_source.rs` (FactSourceDao) + регистрация в lib.rs. Полный тестовый набор — задача 1.15; здесь только smoke-тест. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/fact_source.rs` (`FactSourceDao`: `create`, `get_by_fact_id`, `delete`, `delete_by_fact_id`, `delete_by_document_id`), `crates/db/src/lib.rs` (модуль + re-export), минимальный smoke-тест в `crates/db/src/fact_source.rs` (`#[cfg(test)]`: create + get_by_fact_id round-trip).
  - **Зависимости:** 1.1 (Db, DbError, DbExecutor, test_util), 1.3 (Document), 1.10 (Fact), 1.9 (пул API).
  - **Критерии приёмки:** `cargo build -p db` и `cargo test -p db` зелёные (существующие тесты + smoke); fmt/clippy чисто; публичные API документированы (missing_docs=deny); grep-проверка: нет ссылок на vec0.
  - **Референс:** `../synopsis/internal/database/dao/fact_source_dao.go` — семантика операций; текущий `crates/db/src/entity.rs` — паттерн DAO.
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.8.

- [x] 1.16 Фикс flaky-теста: конкурентные write-тесты на temp-file DB (WAL)
  - **Цель:** устранить флаки `entity::tests::get_or_create_is_atomic_under_concurrency` (SQLITE_LOCKED_SHAREDCACHE 262). Причина (верифицировано реализатором 1.10, A/B): shared-cache `:memory:` + несколько пул-соединений пишут параллельно — busy-handler НЕ работает на shared-cache table-локах (известное ограничение SQLite). Реализация (пул + WAL) корректна для продакшена (file-backed DB, busy_timeout=5000 работает) — чинить ТЕСТ-инфраструктуру, не connection.rs: конкурентные write-тесты должны использовать file-backed DB (WAL), как в проде.
  - **Scope файлов:** `crates/db/src/test_util.rs` (добавить `temp_file_db() -> Db` — временный файл в `std::env::temp_dir()` с уникальным именем (pid+счётчик/UUID), `Db::open`, удаление файла после теста (Drop-обёртка или в конце теста); `in_memory_db` НЕ менять — shared-cache остаётся для неконкурентных тестов), `crates/db/src/entity.rs` (тест `get_or_create_is_atomic_under_concurrency` → `temp_file_db()`), аудит остальных конкурентных тестов на `in_memory_db` (из 1.9: параллельные чтения, чтение во время write-транзакции) — если флакают, перевести на `temp_file_db()`.
  - **Зависимости:** 1.9 (пул), 1.5 (entity), 1.10 (fact — не трогать).
  - **Критерии приёмки:** `cargo test -p db` зелёный; `get_or_create_is_atomic_under_concurrency` проходит 10 повторных прогонов подряд без падений (`for i in $(seq 10); do cargo test -p db -- get_or_create_is_atomic_under_concurrency || exit 1; done`); остальные тесты не затронуты; fmt/clippy чисто.
  - **Референс:** design.md D12 (тест-инфраструктура), текущий `crates/db/src/test_util.rs`, `crates/db/src/entity.rs` (тест).
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): первая версия (решение человека 2026-08-20: исправить тест или реализацию, из-за которой падает).

- [ ] 1.17 Фикс продакшн-бага: list_by_entity_ids (привязка параметров + дедупликация)
  - **Цель:** исправить баг, найденный тестами 1.11: `FactDao::list_by_entity_ids` (crates/db/src/fact.rs:347) передаёт батч-параметры переплетённо `flat_map(|id| [*id, *id])` при SQL с ДВУМЯ IN-списками (subject, object) → позиционная привязка заполняет subject первыми половинами батча, object — вторыми; половина запрошенных id отсутствует в обоих списках, результат неверный и зависит от случайного порядка HashSet. Go-оракул привязывал корректно (`append(args, args...)`). **Баг #2 (замаскирован #1):** при >500 уникальных id факт с endpoints в разных батчах отбирается обоими запросами → дубли в мапе. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** `crates/db/src/fact.rs` (фикс `list_by_entity_ids`: привязка `batch.iter().chain(batch.iter())`; дедупликация фактов при вставке в мапу (проверка уже-добавленного факта по id или HashSet добавленных); снять `#[ignore]` с `list_by_entity_ids_multi_id_regression` и `list_by_entity_ids_batches_over_500_no_duplicates`; расширить тесты (c1)/(c3) на multi-id вход), `crates/db/src/lib.rs` НЕ трогать.
  - **Зависимости:** 1.10 (реализация fact.rs), 1.11 (тесты fact.rs — уже в дереве/истории).
  - **Критерии приёмки:** `cargo test -p db` зелёный; оба regression-теста (multi-id и >500 батч-граница) проходят БЕЗ `#[ignore]`; multi-id вызов `list_by_entity_ids` возвращает факты для ВСЕХ запрошенных id (subject и object), без дублей при >500 id; существующие тесты не сломаны; fmt/clippy чисто; полный workspace зелёный.
  - **Референс:** `../synopsis/internal/database/dao/fact_dao.go` (listByEntityIDs — корректная привязка), текущий `crates/db/src/fact.rs` (строки 324–362 — что чинится), regression-тесты в `crates/db/src/fact.rs` (что раз-игнорируется).
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): первая версия (решение человека 2026-08-20: чинить баг из 1.10 отдельной задачей).

- [ ] 1.15 FactSource DAO — тесты + сборка крейта
  - **Цель:** полный тестовый набор для `fact_source.rs` + финальная сборка крейта: re-exports, документация, полный прогон гейтов. **НЕ транскрибировать Go 1:1** (принцип миграции).
  - **Scope файлов:** тесты в `crates/db/src/fact_source.rs` (`#[cfg(test)]`), `crates/db/src/lib.rs` (все модули + re-exports + crate docs), финальная проверка: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --workspace`, `cargo doc -p db`.
  - **Зависимости:** 1.14 (реализация fact_source.rs), все предыдущие задачи (1.1–1.13).
  - **Критерии приёмки:** `cargo test -p db` зелёный; тесты: (а) fact_source CRUD round-trip; (б) get_by_fact_id; (в) delete_by_fact_id/delete_by_document_id; (г) полный workspace: fmt/clippy/test чисто; (д) `cargo doc -p db` без ошибок (missing_docs=deny соблюдён на всём публичном API); (е) grep-проверка: в крейте db нет ссылок на vec0. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/database/dao/fact_source_dao_test.go` — семантика операций.
  - **История ревизий:**
    - Ревизия 1 (2026-08-20): выделена из 1.8.