# ingestion-sources — Tasks

Порядок = граф зависимостей: 1.1 → {1.2, 1.4} → 1.3 → {1.5, 1.6} → 1.7 → {1.8, 1.9, 1.10} → 1.11. Каждая задача выполняется СВЕЖИМ агентом без памяти предыдущих — тело самодостаточно. Формат: чекбокс + блок деталей (цель / scope / зависимости / критерии приёмки / референс / история ревизий). **Принцип миграции (binding):** НЕ транскрибировать Go 1:1 — функциональная копия; архитектурно правильно для Rust (DRY, KISS, SOLID, YAGNI); внутренняя совместимость с оракулом не требуется; баги Go исправлять или фиксировать осознанные отклонения. CI без сети.

Общие факты для всех задач (источники истины): design этого change'а D1–D6; config-крейт: `ChunkingConfig` (`markdown.max_chunk_size` default 1000, `markdown.overlap_size` default 100 — configured 0 сохраняется; json-секция), global.xml-парсер (типы источников); оракул: `internal/ingestion/types.go` (Document/ParseResult/Parser), `sources/{source.go,registry.go}`, `parsers/*.go`, `chunkers/*.go`. Чанк БЕЗ NerResult (design D2). Гейты каждой задачи: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test -p ingestion`, `cargo test --workspace`; missing_docs=deny, unsafe_code=forbid; пин 1.96.0. Untracked `.idea/`, `.opencode/opencode.json`, `.opencode/plans/`, skills-lock.json не трогать.

---

- [x] 1.1 Скаффолдинг крейта: трейты и типы
  - **Цель:** создать `crates/ingestion` с трейтами Parser/Chunker/Source и типами Document/ParseResult/DocumentChunk/Metadata.
  - **Scope файлов:** `crates/ingestion/Cargo.toml` (+config), `crates/ingestion/src/lib.rs`, `src/types.rs`, `src/error.rs`; корневой Cargo.toml при необходимости.
  - **Детали:** сигнатуры по design D1; DocumentChunk = {doc_id: Option<i64>, text, sequence_num, start_offset/end_offset: usize (byte), metadata}; Metadata — типизированная базовая структура + extension-поле (детали по усмотрению, задокументировать); ParseResult {documents, errors: Vec<IngestionError>} — нефатальные ошибки вместе с документами.
  - **Критерии приёмки:** гейты зелёные; компиляционные тесты трейтов (объект-безопасность dyn Source); юнит-тесты типов (офсеты на UTF-8 строке).
  - **Зависимости:** нет.
  - **Референс:** design D1/D2; internal/ingestion/types.go.

- [x] 1.2 Markdown-парсер
  - **Цель:** обнаружение `.md`/`.markdown` файлов и извлечение документов.
  - **Scope файлов:** `src/parsers/markdown.rs` (+тесты, +фикстуры при необходимости).
  - **Детали:** семантика обхода/чтения — сверить с ../synopsis/internal/ingestion/parsers/markdown_parser.go (+его тесты): что попадает в Metadata (file_size, modified_at…), как обрабатываются ошибки чтения отдельных файлов (нефатально), рекурсивный ли обход.
  - **Критерии приёмки:** тесты на временных деревьях файлов: обнаружение обоих расширений, metadata наполнена, ошибка одного файла не рвёт обход; гейты зелёные.
  - **Зависимости:** 1.1.
  - **Референс:** parsers/markdown_parser.go.

- [x] 1.2b Поддержка .synignore (замена SKIP_DIRS)
  - **Ревизия (2026-08-23, решение человека):** hardcoded SKIP_DIRS из задачи 1.2 УДАЛЯЕТСЯ полностью — исключение каталогов/файлов делается исключительно через `.synignore`-файлы (gitignore-семантика) посредством крейта `ignore` (BurntSushi/ripgrep; кастомное имя файла через add_custom_ignore_filename). Один механизм, а не два пересекающихся (DRY/KISS). Следствие принято: без .synignore обходится всё (источники — явно сконфигурированные пути в global.xml).
  - **Цель:** gitignore-подобные пользовательские исключения на уровне общего walk-хелпера — все парсеры наследуют автоматически.
  - **Scope файлов:** `crates/ingestion/Cargo.toml` (+ignore workspace pin после верификации MSRV/features по registry), корневой Cargo.toml, `src/parsers/mod.rs` (walk-хелпер), тесты.
  - **Детали:** .synignore читается из корня источника И из подкаталогов (gitignore-семантика вложенности); built-in дефолтов НЕТ (SKIP_DIRS удалён); отсутствие .synignore — валидный случай (обход всего). Верифицировать API крейта ignore по исходникам registry (~/.cargo/registry/src/*/ignore-*/): IgnoreBuilder::add_custom_ignore_filename, walk-интеграция или match-по-пути — выбрать чистейший вариант для нашего рекурсивного std::fs-обхода (детерминированный порядок сортировки СОХРАНИТЬ).
  - **Критерии приёмки:** тесты: .synignore в корне исключает каталог/файл/по-паттерну; вложенный .synignore в подкаталоге работает; без файла обходится всё; детерминизм порядка сохранён; гейты зелёные.
  - **Зависимости:** 1.2.
  - **Референс:** crates.io/docs.rs ignore (BurntSushi); решение человека 2026-08-23.

- [x] 1.3 Markdown-чанкер (structure-aware)
  - **Цель:** деление по секциям заголовков с max_chunk_size/overlap из конфига.
  - **Scope файлов:** `src/chunkers/markdown.rs` (+тесты +фикстуры).
  - **Детали:** семантика — markdown_chunker.go и ЕГО тесты (ожидания границ зафиксированы там): заголовки каких уровней режут, как overlap отступает от границы секции, metadata.section_title. Byte-офсеты валидны: text[start..end] == chunk.text. overlap=0 сохраняется (config-крейт уже нормализует дефолты — чанкер не подменяет).
  - **Критерии приёмки:** тесты: многоуровневые заголовки, длинная секция > max_chunk_size режется внутри с overlap, overlap=0, офсеты-инвариант на UTF-8 кириллице; дифференциальный тест против фикстуры/ожиданий оракула; гейты зелёные.
  - **Зависимости:** 1.2.
  - **Референс:** design D4; chunkers/markdown_chunker.go.

- [x] 1.4 JSON-парсер
  - **Цель:** обработка .json согласно семантике оракула.
  - **Scope файлов:** `src/parsers/json.rs` (+тесты +фикстуры).
  - **Детали:** сверить с parsers/json_parser.go: структура JSON-документов, ожидаемых ингестией (массив объектов? поля?), что становится Content документа.
  - **Критерии приёмки:** тесты по фикстурам оракула; битый JSON — нефатальная ошибка в ParseResult; гейты зелёные.
  - **Зависимости:** 1.1.
  - **Референс:** parsers/json_parser.go.

- [x] 1.5 JSON-чанкер
  - **Цель:** деление JSON-содержимого по структуре документа.
  - **Scope файлов:** `src/chunkers/json.rs` (+тесты).
  - **Детали:** сверить с chunkers/json_chunker.go (+тесты): единица членения (объект массива?), офсеты, metadata.
  - **Критерии приёмки:** тесты по фикстурам; офсеты-инвариант; гейты зелёные.
  - **Зависимости:** 1.4.
  - **Референс:** chunkers/json_chunker.go.

- [x] 1.6 Реестр источников
  - **Цель:** тип из global.xml → реализация Source; диспетчеризация расширений.
  - **Scope файлов:** `src/sources.rs` (или registry.rs) (+тесты).
  - **Детали:** сверить с sources/registry.go: регистрация, lookup по type, поведение при неизвестном типе (явная ошибка). MarkdownSource/JsonSource как первые композиты.
  - **Критерии приёмки:** тесты: известные типы возвращают реализации; неизвестный — ошибку; гейты зелёные.
  - **Зависимости:** 1.3, 1.5.
  - **Референс:** design D5; sources/registry.go.

- [x] 1.7 Mediawiki-источник (parser+chunker)
  - **Цель:** формат mediawiki по семантике оракула.
  - **Scope файлов:** `src/parsers/mediawiki.rs`, `src/chunkers/mediawiki.rs` (+тесты+фикстуры), регистрация в реестре.
  - **Детали:** сверить с parsers/mediawiki_parser.go и chunker'ом (если выделен) — чем отличается от markdown (синтаксис `== Заголовок ==`?).
  - **Критерии приёмки:** тесты по фикстурам оракула; гейты зелёные.
  - **Зависимости:** 1.6.
  - **Референс:** parsers/mediawiki_parser.go.

- [x] 1.8 Webpage-источник (parser+chunker)
  - **Цель:** формат webpage (HTML?) по семантике оракула.
  - **Scope файлов:** parser+chunker webpage (+тесты+фикстуры), регистрация.
  - **Детали:** сверить с parsers/webpage_parser.go: извлечение текста из HTML (какая стратегия?), зависимости (html-крейт? проверить, что уже в палитре или добавить с верификацией).
  - **Критерии приёмки:** тесты по фикстурам; гейты зелёные.
  - **Зависимости:** 1.6.
  - **Референс:** parsers/webpage_parser.go.

- [ ] 1.9 Unstructured-источник (parser+chunker)
  - **Цель:** формат unstructured (plain text?) по семантике оракула.
  - **Scope файлов:** parser+chunker unstructured (+тесты), регистрация.
  - **Детали:** сверить с parsers/unstructured_parser.go.
  - **Критерии приёмки:** тесты; гейты зелёные.
  - **Зависимости:** 1.6.
  - **Референс:** parsers/unstructured_parser.go.

- [ ] 1.10 Паритет-фикстуры всех форматов
  - **Цель:** единый набор фикстур оракула + зафиксированные ожидания (число чанков, тексты) для дифференциальных тестов.
  - **Scope файлов:** `crates/ingestion/tests/parity_fixtures.rs` (+каталог fixtures/).
  - **Детали:** скопировать testdata оракула (internal/ingestion/runner/testdata + файлы тестов парсеров); ожидания взять из Go-тестов оракула (они фиксируют число чанков/структуру); один параметризованный тест на формат.
  - **Критерии приёмки:** паритет-тесты всех 5 форматов зелёные; расхождения задокументированы как отклонения либо исправлены; гейты зелёные.
  - **Зависимости:** 1.7–1.9.
  - **Референс:** design D6; AGENTS.md «Паритет проверяется машиной».

- [ ] 1.11 Финальная сборка крейта
  - **Цель:** re-exports публичного API, целостность docs, финальные гейты.
  - **Scope файлов:** lib.rs (ре-экспорты), crate docs.
  - **Критерии приёмки:** cargo doc -p ingestion без предупреждений; полный workspace зелёный; публичное API покрытие: все 5 Source-реализаций доступны из корня.
  - **Зависимости:** 1.10.
  - **Референс:** конвенция lib.rs соседних крейтов.
