# Tasks — config-module

Порядок = граф зависимостей: 1.1 → 1.2 → 2.1 → 3.1 → 3.2 (приоритет yaml > onnx > xml, решение человека). Каждая задача выполняется СВЕЖИМ агентом без памяти предыдущих — тело самодостаточно. Формат: чекбокс + блок деталей (цель / scope / зависимости / критерии приёмки / референс / история ревизий).

**Решения change (2026-08-18, одобрено человеком):** D1 serde-структуры без builder; D2 noyalib 0.0.23 (в палитре); D3 quick-xml (event-based, ручной мэппинг; пин в crates/config/Cargo.toml после онлайн-проверки); D4 thiserror (в палитру); D5 regex компилируется при загрузке (в палитру); D6 глобальный пул — слои вместо merge (один парсер global.xml; shadowing без warning; union-валидация и lookup — в change `graph`); D7 enum'ы: строгие где Go валидирует, толерантные `Unknown(String)` где пропускает; D8 auto_update через `Option`; D9 модули lib.rs/error.rs/preset.rs/onnx.rs/ontology.rs/domain.rs; D10 фикстуры в `crates/config/tests/data/` (НЕ `fixtures/` — gitignored на любом уровне) с README-provenance; D11 thiserror+regex в палитру, quick-xml локально.

## 1. YAML-пресет

- [x] 1.1 Скелет крейта + структуры YAML-пресета + load + validate
  - **Цель:** рабочий каркас `crates/config` и полный набор serde-структур главного конфига с парсингом и валидацией (без дефолтов — задача 1.2). Поведение парсинга совпадает с оракулом: неизвестные ключи не ломают старт; enum'ы по D7.
  - **Scope файлов:** `crates/config/Cargo.toml` (deps: `serde`, `noyalib`, `thiserror` через workspace), `crates/config/src/lib.rs` (crate docs + re-exports), `crates/config/src/error.rs` (`ConfigError` через thiserror: Io{path}, Yaml{path,source}, Validation{message} — варианты для XML/regex добавят задачи 3.x), `crates/config/src/preset.rs` (все структуры + `load(path)` + `validate()`), фикстура `crates/config/tests/data/config.default.yaml` (копия из `../synopsis/configs/config.default.yaml` verbatim) + `crates/config/tests/data/README.md` (provenance: источник, дата копирования, команда регенерации, sha256). Корневой `Cargo.toml`: добавить `thiserror` в палитру с комментарием «версия проверена онлайн реализатором (MSRV/CVE)».
  - **Структуры (по config.go):** `Config` (database, embeddings, ingestion, linker, search, graph, `auto_update: Option<AutoUpdateConfig>` — D8, scheduler, logging, paths, server); `DatabaseConfig` (path, cache_path, pragma: HashMap<String,String>); `EmbeddingsConfig` (mode: `EmbeddingsMode` enum local|api — строгий, local: `LocalEmbedding`, api: `APIEmbedding`, auto_rebuild_vectors); `IngestionConfig` (chunking: markdown/json, ner: prose/llm, batch_size, resolver); `ChunkingConfig`/`MarkdownChunkerConfig` (strategy: `ChunkingStrategy` — толерантный, max_chunk_size, overlap_size, min_section_size)/`JsonChunkerConfig` (text_fields, combine_fields, max_objects); `NerConfig` (disabled, prose: `ProseNerConfig`, llm: `LlmConfig`); `LlmConfig` (api_base_url, api_key, model_name, temperature, max_tokens, seed, response_format: `ResponseFormat` — толерантный, timeout_ms, max_retries); `ResolverConfig` (similarity_threshold); `LinkerConfig` (disabled, llm: `LlmConfig`); `SearchConfig` (rrf_k, lexical_top_k, semantic_top_k, final_top_k, enable_lexical, enable_semantic, timeout_ms, deprecated_boost, official_boost, recent_boost, recent_days, authority_boost: HashMap<String,f64>); `GraphConfig` (enable_graph, max_depth, max_nodes, load_on_startup); `AutoUpdateConfig` (enabled, debounce_seconds, watch_sources, initial_sync); `SchedulerConfig` (jobs: HashMap<String,JobConfig>); `JobConfig` (enabled, interval_seconds); `LoggingConfig` (level: `LogLevel`, format: `LogFormat`, output: `LogOutput` — все толерантные); `PathsConfig` (data_dir, documents_dir, migrations_dir, global_config_path, prompts_path, onnx_config); `ServerConfig` (name, version, host, port). Толерантные enum'ы: `#[serde(rename_all = "lowercase")]` + вариант `Unknown(String)` с `#[serde(untagged)]`-подобным приёмом (или `#[serde(other)]`-совместимым — выбрать рабочий для noyalib; НЕЛЬЗЯ `deny_unknown_fields`). `load(path) -> Result<Config, ConfigError>`: read + noyalib parse. `validate(&self) -> Result<(), ConfigError>`: ровно проверки config.go Validate (mode local → model_path или model_name непуст + vector_dim > 0; mode api → base_url/model_name непусты + vector_dim > 0; иначе — ошибка «unknown embeddings mode»).
  - **Зависимости:** нет (первая задача change'а). Палитра уже содержит serde/noyalib.
  - **Критерии приёмки:** `cargo test -p config` зелёный; тесты: (а) парсинг фикстуры config.default.yaml — все секции распознаны, значения совпадают с файлом; (б) неизвестный ключ в YAML не вызывает ошибку; (в) незнакомое значение `logging.level` (напр. "verbose") парсится в `Unknown` без ошибки; (г) `embeddings.mode: "bogus"` → Validation-ошибка; (д) local-режим без model_name/model_path и vector_dim=0 → ошибка; (е) api-режим без base_url → ошибка; (ж) `ConfigError` реализует Display + std::error::Error (source для Yaml). `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings` — чисто; `missing_docs` соблюдён на публичном API.
  - **Референс:** `../synopsis/internal/config/config.go` (структуры, Load, Validate), `../synopsis/configs/config.default.yaml`, тесты `../synopsis/internal/config/config_test.go` (семантика валидации).

- [x] 1.2 apply_defaults + хелперы + полный тест-сьют
  - **Цель:** дефолты и производные хелперы конфига, поведение идентично оракулу (включая auto_update-presence по D8), полное тестовое покрытие на фикстуре.
  - **Scope файлов:** `crates/config/src/preset.rs` (добавить `apply_defaults(&mut self)`, `vector_dim()`, `db_path()`, `cache_db_path()`), `crates/config/src/lib.rs` (re-export при необходимости), тесты в `crates/config/src/preset.rs` (модуль `#[cfg(test)]`) и/или `crates/config/tests/`.
  - **apply_defaults — точный перенос значений config.go ApplyDefaults:** batch_size=100; chunking.markdown: strategy="headers" (если пусто), max_chunk_size=1000, overlap_size=100 (если <0), min_section_size=500; json.text_fields=["description","title","wikitext","html"] (если пусто); search: rrf_k=20, lexical_top_k=20, semantic_top_k=20, final_top_k=10, если оба enable_* false → оба true, timeout_ms=10000, deprecated_boost=0.2, official_boost=1.5, recent_boost=1.2, recent_days=90, authority_boost={"default":1.0} (если пусто); graph: max_depth=5, max_nodes=1000, load_on_startup=true (если false); auto_update: debounce_seconds=30, watch_sources=true, и если `auto_update: None` → enabled=true, initial_sync=true (D8 — НЕ сканировать YAML-ноды, как Go); scheduler: jobs пустой map не nil; orphan_cleanup: если отсутствует → enabled=false, interval_seconds=3600 (внимание: в Go дефолт enabled=false, НЕ true — см. комментарий в config.go); logging: level="info", format="console", output="stderr"; paths: data_dir="data", documents_dir="documents", migrations_dir="migrations", prompts_path="configs/prompts", onnx_config="configs/onnx.yaml"; ner.llm: response_format="json_object", timeout_ms=60000, max_retries=3; resolver.similarity_threshold=0.8; embeddings.local: если model_name и model_path пусты → model_name="bge-m3-int8"; server: name="synopsis", version="0.1.0-dev", host="0.0.0.0", port=8080. Хелперы: `vector_dim()` — по mode (local→local.vector_dim, api→api.vector_dim, иначе 0); `db_path()` — database.path или `data_dir/knowledge.db`; `cache_db_path()` — database.cache_path или `db_path-дир/cache.db`.
  - **Зависимости:** 1.1.
  - **Критерии приёмки:** `cargo test -p config` зелёный; тесты: (а) пустой `Config::default()` после apply_defaults даёт все дефолты выше (сверка значений с config.go); (б) фикстура config.default.yaml: apply_defaults не меняет явно заданные значения; (в) auto_update: None → enabled/initial_sync=true; Some(enabled=false) → остаётся false; (г) db_path/cache_db_path: явный path, fallback через data_dir, cache_db_path рядом с db; (д) vector_dim по обоим режимам; (е) orphan_cleanup: отсутствует → enabled=false/3600; задан явно → уважается. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/config/config.go` (ApplyDefaults, DBPath, CacheDBPath, VectorDim), `../synopsis/internal/config/config_test.go`.
  - **История ревизий:**
    - Ревизия 1 (2026-08-19, решения человека — исправление 1:1-транскрипции, см. design D12/D13): (а) **bool-поля с presence-семантикой** — `graph.load_on_startup`, `auto_update.watch_sources`, `graph.enable_graph`: `#[serde(default = "default_true")]` + Default impl (absent → true, явный false → false). Исправляет баги Go (`if !x { x = true }` — настройки были нерабочими; enable_graph — дефолт по интенту доки). BREAKING-сценарии добавлены в спек. (б) **Пустые строки нормализуются при десериализации**: tolerant_enum `""` → Default-вариант; 8 строковых полей (`paths.data_dir/documents_dir/migrations_dir/prompts_path/onnx_config`, `server.name/version/host`) — `#[serde(default = "fn", deserialize_with = "de_empty_to_default")]`. apply_defaults теряет `== ""`-проверки для этих полей. (в) **Граница**: числа `<= 0` (кроме `overlap_size` — `< 0`), условные пары (`model_name`/`model_path`, `enable_lexical`/`enable_semantic`), мапы (`authority_boost`, `orphan_cleanup`), presence секции auto_update — остаются в apply_defaults. (г) Тесты обновить под новую семантику: явный false уважается; absent → true; `""` → дефолт; `Config::default()` + apply_defaults по-прежнему даёт полный дефолтный конфиг.
    - Ревизия 2 (2026-08-19, решение человека): **EmbeddingsMode при отсутствующем ключе остаётся `Local`** (вариант B) — осознанное отклонение от оракула (Go: пустой mode отклоняется в Validate). Зафиксировано; parity-тесты на config.default.yaml не затрагиваются (mode задан явно).

## 2. Реестр onnx.yaml

- [ ] 2.1 onnx.yaml: структуры, загрузка, lookup'и
  - **Цель:** реестр моделей ONNX: парсинг `onnx.yaml`, ошибка при отсутствии файла (сценарий спека), lookup платформы и модели.
  - **Scope файлов:** `crates/config/src/onnx.rs` (структуры + `load_onnx_config(path) -> Result<OnnxConfig, ConfigError>` + `platform_for_key(&self, key) -> Option<&OnnxPlatformConfig>` + `model_for_name(&self, name) -> Option<&ModelInfo>`), `crates/config/src/lib.rs` (модуль + re-exports), `crates/config/src/error.rs` (добавить вариант `Onnx { path, source }` при необходимости), фикстура `crates/config/tests/data/onnx.yaml` (копия из `../synopsis/configs/onnx.yaml` verbatim) + дополнение README (provenance).
  - **Структуры (по config.go):** `OnnxConfig` (runtime: `OnnxRuntimeConfig` {version, platforms: Vec<OnnxPlatformConfig>}, models: `OnnxModelsConfig` {default, entries: Vec<ModelInfo>}); `OnnxPlatformConfig` (key, os, arch, archive_url, archive_format: `ArchiveFormat` — толерантный enum zip|tgz|Unknown, library_name, library_path); `ModelInfo` (name, display_name, description, version, vector_dim, files: Vec<ModelFile>, source, repo); `ModelFile` (name, url, size_bytes: i64, checksum: Option<String>). `load_onnx_config`: read + parse; отсутствующий файл → `ConfigError::Io` с путём (как Go: «read onnx config %s»). Lookup'и — по ключу/имени, Option.
  - **Зависимости:** 1.1 (error.rs, палитра).
  - **Критерии приёмки:** `cargo test -p config` зелёный; тесты: (а) фикстура onnx.yaml парсится: 5 платформ, 3 модели, default="bge-small-en-v1.5", размеры файлов совпадают с файлом; (б) `platform_for_key("linux-amd64")` → Some с archive_url из файла; неизвестный ключ → None; (в) `model_for_name("bge-m3-int8")` → Some (vector_dim=1024, 3 файла); неизвестное имя → None; (г) отсутствующий файл → ошибка с путём; (д) невалидный YAML → ошибка парсинга. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/config/config.go` (LoadONNXConfig, PlatformForKey, ModelForName, ONNX-структуры), `../synopsis/configs/onnx.yaml`, `../synopsis/configs/onnx.yaml.default` (идентичен — sanity-чек).

## 3. Онтологии XML

- [ ] 3.1 global.xml: один парсер, sources/cross-domain-links/ner + пул, валидация
  - **Цель:** единый парсер `global.xml` (D6 — в Go файл парсится дважды: config.LoadGlobalConfig и domain.LoadGlobalPool; у нас одна структура `GlobalConfig` со ВСЕМИ блоками), дефолты и валидация как в оракуле, regex-правила компилируются при загрузке (D5).
  - **Scope файлов:** `crates/config/Cargo.toml` (deps: `quick-xml` — версия/MSRV/CVE проверены онлайн реализатором, пин с комментарием; `regex` через workspace), корневой `Cargo.toml` (добавить `regex` в палитру с комментарием о проверке), `crates/config/src/ontology.rs` (структуры + `load_global_config(ontology_dir) -> Result<Option<GlobalConfig>, ConfigError>`), `crates/config/src/error.rs` (варианты `Xml { path, source }`, `Regex { file, rule, source }`), `crates/config/src/lib.rs`, фикстура `crates/config/tests/data/global.xml` (копия verbatim) + README.
  - **Структуры (по global_config.go + global_pool.go):** `GlobalConfig` {sources: Vec<SourceConfig>, cross_domain_links: Option<CrossDomainLinksConfig>, ner: GlobalNerConfig, entities: Vec<EntityDef>, relations: Vec<RelationDef>, extraction: ExtractionDef}; `SourceConfig` (path, type: `SourceType` — толерантный enum markdown|webpages|mediawiki|unstructured|Unknown, disabled, space, domains: Vec<String>, dataset); `CrossDomainLinksConfig` (methods: Vec<LinkMethod> — СТРОГИЙ enum expression|equals|llm, equals: Option<EqualsConfig> {min_words}, llm_confidence_threshold: f64, batch_size: i32, expressions: Vec<LinkExpression> {name, description, priority, where_, relation_type}); `GlobalNerConfig` (methods: Vec<NerMethod> — СТРОГИЙ enum regex|prose|llm); `EntityDef` (id, name, description, attributes: Vec<AttributeDef>, synonyms: Vec<String>); `AttributeDef` (name, type: `AttributeType` — толерантный enum string|date|number|ref|boolean|Unknown, required, target); `RelationDef` (source, predicate, target, description, attributes: Vec<RelAttrDef>); `RelAttrDef` (name, type); `ExtractionDef` (regex_rules: Vec<RegexRuleDef>); `RegexRuleDef` (id, entity, pattern, confidence, compiled: regex::Regex). Парсинг — quick-xml event-based (D3), ошибки с контекстом файла. `load_global_config`: пустой ontology_dir → Ok(None); отсутствующий global.xml → Ok(None) (как Go); иначе parse + дефолты + валидация. Дефолты (global_config.go): cross_domain_links: equals.min_words=2 (если ≤0), llm_confidence_threshold=0.7, batch_size=5, expression.relation_type="same_entity" (если пусто); ner: методы по умолчанию ["regex","llm"] если блок отсутствует. Валидация: link-методы ∈ {expression,equals,llm} и непусты; ner-методы ∈ {regex,prose,llm} и непусты; каждый source: path и type непусты, пустой domains → ["default"]; пул (global_pool.go Validate): уникальные entity ID, уникальные имена атрибутов, ref-атрибут с пустым target → ошибка, relation source/target существуют среди entity пула, каждый regex компилируется (D5) — ошибка с файлом и id правила.
  - **Зависимости:** 1.1 (error.rs, стиль). Новые deps: quick-xml (локально), regex (палитра).
  - **Критерии приёмки:** `cargo test -p config` зелёный; тесты: (а) фикстура global.xml: 8 sources (типы markdown/webpages/mediawiki), 3 link-метода, equals.min_words=2, 1 expression (priority=80, relation-type same_entity), ner=["regex","llm"], 5 entity, 6 relation, 1 regex-правило (compiled); (б) отсутствующий файл/пустая директория → Ok(None); (в) невалидный метод (напр. "bogus" в <method>) → ошибка валидации; (г) source без path → ошибка; (д) невалидный regex-pattern → `ConfigError::Regex` с именем файла и id; (е) невалидный XML → `ConfigError::Xml` с путём; (ж) дубликат entity ID → ошибка. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/config/global_config.go` (+ global_config_test.go), `../synopsis/internal/domain/global_pool.go` (+ global_pool_test.go), `../synopsis/data/ontology/global.xml`.

- [ ] 3.2 domain-XML: структуры домена, загрузка, валидация
  - **Цель:** парсинг `domains/*.xml` в типизированные структуры с валидацией и компиляцией regex при загрузке (D5); невалидный XML или regex — ошибка старта (сценарии спека).
  - **Scope файлов:** `crates/config/src/domain.rs` (структуры + `load_domain_config(path) -> Result<DomainConfig, ConfigError>`), `crates/config/src/lib.rs`, фикстуры `crates/config/tests/data/domains/domain_hr.xml`, `domain_it.xml`, `domain_product.xml` (копии verbatim) + README. (Сканирование директории domains/ и registry-логика — change `graph`, здесь только per-file загрузка.)
  - **Структуры (по domain_config.go):** `DomainConfig` {name, version, description, entities: Vec<EntityDef>, relations: Vec<RelationDef>, extraction: ExtractionDef, confidence: ConfidencePolicy}; `EntityDef`/`AttributeDef`/`RelationDef`/`RelAttrDef`/`ExtractionDef`/`RegexRuleDef` — переиспользовать типы из `ontology.rs` (общие для пула и доменов; вынести в общий модуль, если чище — решение реализатора в рамках scope); `ConfidencePolicy` (auto_publish_threshold, review_threshold, reject_threshold — f64, атрибуты XML). `load_domain_config`: read + parse; отсутствующий файл → ошибка с путём; валидация per-file: уникальные entity ID, уникальные имена атрибутов, ref-атрибут с пустым target → ошибка, relation source/target существуют среди entity ДОМЕНА, каждый regex компилируется (ошибка с файлом и id). Дефолты confidence-порогов (0.85/0.60/0.40) — если атрибуты отсутствуют (проверить семантику в domain_config.go: дефолты применяются при загрузке или позже — следовать оракулу).
  - **Зависимости:** 3.1 (типы EntityDef/AttributeDef/RelationDef/ExtractionDef/RegexRuleDef, quick-xml, regex).
  - **Критерии приёмки:** `cargo test -p config` зелёный; тесты: (а) все 3 фикстуры парсятся: domain_hr.xml — entity/relation/extraction/confidence распознаны (сверка с файлом); (б) отсутствующий файл → ошибка с путём; (в) невалидный XML → `ConfigError::Xml`; (г) невалидный regex → `ConfigError::Regex` с файлом и id; (д) дубликат entity ID в домене → ошибка; (е) relation на несуществующий entity → ошибка. fmt/clippy чисто.
  - **Референс:** `../synopsis/internal/domain/domain_config.go` (+ domain_config_test.go), `../synopsis/data/ontology/domains/*.xml`.