/.idea
/target
# Standalone build artifacts of the archived spikes crate (task 5.1).
.archive/spikes/target/
*.rs.bk

# Runtime artifacts under workspace/ (storage-layout-restructure task 1.4):
# ONNX Runtime .so (~24 MB), model files (~2.3 GB), the global cache DB, and
# per-dataset state (knowledge.db + vectors) are all gitignored; provenance and
# verification live in workspace/README.md.
workspace/*
!workspace/configs/
!workspace/datasets/edtech/ontology/
!workspace/datasets/edtech/content/

# Cargo.lock is committed on purpose: this workspace produces a binary and
# reproducible builds matter for parity testing against recorded fixtures.

# Docusaurus website build artifacts
docs/website/node_modules/
docs/website/.docusaurus/
docs/website/build/