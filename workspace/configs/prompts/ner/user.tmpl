Extract entities and relations from the provided content.

{% if entity_types %}
---
Entity types to extract: {{ entity_types | join(", ") }}
{% endif %}
{% if relation_types %}
---
Relation types to extract: {{ relation_types | join(", ") }}
{% endif %}
---
{% if section_path %}
CONTENT SECTION
```
{{ section_path }}
```
{% endif %}
CONTENT:
```
{{ content }}
```
---
IMPORTANT
- Only extract entities and relations that are explicitly mentioned or clearly implied
- Use the exact entity type IDs and relation type IDs from the schema
- Return valid JSON even if nothing is found (use empty arrays)
- Use the domain schema to guide your extraction.
- Return only valid JSON in the specified format
